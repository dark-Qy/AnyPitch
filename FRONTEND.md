# FRONTEND.md

## Design Direction

- `purpose`: 让教练把队员、训练、友谊赛、出勤和战术部署放在同一个可执行工作台
- `audience`: 单支业余或半职业球队的教练、队长
- `tone`: 参考 Taplite 的暖纸网格、粗黑边框、低圆角和信号绿强调，落成紧凑型战术板工作台
- `differentiation`: 纵向真实比例球场和可叠加对手站位是第一视觉记忆点，但整体像清爽工作台而不是暗色仪表盘

## Current UI

- `/auth` 等价入口由前端状态控制，提供 `教练` / `队员` 双入口
- `教练` 使用邮箱和密码登录，密码可由服务端 `ANYPITCH_COACH_PASSWORD` 覆盖
- `队员` 只输入已在队员列表中的姓名，无需密码
- 登录后是顶部工作台导航 + 主工作区，手机端压缩为四个图标入口
- `战术板` 先选 5/8/11 赛制，再选该赛制下的中文模板，并使用 dnd-kit 拖拽 slots
- `战术板` 可开启对手站位；对手模板跟随当前赛制并镜像到另一半场，可独立拖拽
- `战术板` 使用常规足球场纵向比例和圆形球员点，避免方牌占用过多球场空间
- `队员` 教练侧提供姓名、号码、中文位置和状态的增删改查
- 顶部品牌主标题使用队名 `西土城FC`
- `队员入口` 只展示日程表、参加/拒绝/待定/未确认汇总和自己的参加状态，可选择可参加 / 不可参加 / 待定 / 未确认
- `日程` 使用可点击月历管理训练和友谊赛时间段，可填写训练内容等备注，地点默认北京邮电大学（海淀校区）
- `日程` 支持维护常用地点，并在新增日程时通过下拉选择
- `日程` 点击具体日程后在详情面板直接编辑训练内容备注并登记队员出勤
- `出勤` 保留独立入口，用于按日程快速批量登记队员状态

## Rules

- 不做营销 landing page，首屏就是可用工作台
- 不放说明性大标题，导航和当前面板自己说明功能，优先让战术板、表单和列表在首屏露出
- 使用 CSS variables 管理颜色、阴影和版面 token
- 所有按钮、输入框、战术板 marker 必须有手机端可触摸尺寸
- 前端只缓存 bearer token 和 UI 状态，不保存业务真相
- 固定文案使用简体中文
- 下拉控件统一使用自定义箭头位置，避免系统箭头贴边或在移动端漂移
